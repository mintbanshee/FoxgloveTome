// src/app/dashboard

import DashboardClient from "../../components/DashboardClient/DashboardClient";

// *~*~*~*~*~*~* RENDER DASHBOARD *~*~*~*~*~*~*

// call the dashboard client component to render the dashboard page
export default function DashboardPage() {
  return (
    <div>
      <DashboardClient />
    </div>
  );
}